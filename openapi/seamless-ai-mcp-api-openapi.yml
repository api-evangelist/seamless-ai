openapi: 3.2.0
info:
  title: Seamless.AI Server MCP API
  version: 1.0.0
  x-logo:
    url: https://s3.amazonaws.com/seamless.ai-public/logos/logo-full-dark.svg
    altText: Seamless logo
    href: https://login.seamless.ai
  description: Let your AI agents interact with Seamless.AI by using our MCP server.
servers:
- url: https://mcp.seamless.ai
  description: Production
security:
- OAuth2:
  - mcp.all
- ApiKey: []
tags:
- name: MCP
paths:
  /mcp:
    post:
      operationId: mcp_transport
      summary: MCP Transport
      description: 'The single transport endpoint for all MCP communication. All tool calls, resource reads, and protocol messages are sent here as JSON-RPC 2.0 requests.


        This is a stateless endpoint — each request creates a fresh server instance scoped to the authenticated user.'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
              - jsonrpc
              - method
              - id
              properties:
                jsonrpc:
                  type: string
                  enum:
                  - '2.0'
                  description: JSON-RPC version. Always "2.0".
                method:
                  type: string
                  description: MCP method to invoke (e.g. "tools/list", "tools/call", "resources/read").
                  example: tools/call
                params:
                  type: object
                  description: Method-specific parameters. For `tools/call`, includes `name` (tool name) and `arguments` (tool input).
                  properties:
                    name:
                      type: string
                      description: Tool name (for tools/call).
                      example: search_contacts
                    arguments:
                      type: object
                      description: Tool input arguments (for tools/call).
                      additionalProperties: true
                id:
                  oneOf:
                  - type: string
                  - type: integer
                  description: Request ID for correlating responses.
            examples:
              listTools:
                summary: List available tools
                value:
                  jsonrpc: '2.0'
                  method: tools/list
                  params: {}
                  id: 1
              callTool:
                summary: Call a tool
                value:
                  jsonrpc: '2.0'
                  method: tools/call
                  params:
                    name: search_contacts
                    arguments:
                      companyName:
                      - Acme Corp
                      seniority:
                      - VP
                      - Director
                      limit: 10
                  id: 2
              readResource:
                summary: Read a resource
                value:
                  jsonrpc: '2.0'
                  method: resources/read
                  params:
                    uri: seamless://credits
                  id: 3
      responses:
        '200':
          description: JSON-RPC 2.0 response with the result or error.
          content:
            application/json:
              schema:
                type: object
                properties:
                  jsonrpc:
                    type: string
                    example: '2.0'
                  result:
                    type: object
                    description: Method-specific result payload.
                  error:
                    type: object
                    description: Error object if the request failed.
                    properties:
                      code:
                        type: integer
                      message:
                        type: string
                  id:
                    oneOf:
                    - type: string
                    - type: integer
              examples:
                toolResult:
                  summary: Successful tool call
                  value:
                    jsonrpc: '2.0'
                    result:
                      content:
                      - type: text
                        text: Found 15 contacts matching your search...
                    id: 2
                errorResult:
                  summary: Error response
                  value:
                    jsonrpc: '2.0'
                    error:
                      code: -32602
                      message: 'Invalid params: companyName must be an array'
                    id: 2
        '401':
          description: Missing or invalid authentication.
        '405':
          description: Method not allowed (e.g. DELETE request).
      tags:
      - MCP
components:
  securitySchemes:
    OAuth2:
      type: oauth2
      flows:
        authorizationCode:
          authorizationUrl: https://mcp.seamless.ai/mcp/authorize
          tokenUrl: https://mcp.seamless.ai/mcp/token
          scopes:
            mcp.all: Full MCP access
    ApiKey:
      type: apiKey
      name: Token
      in: header
      description: API key passed via the `Token` header.
